# Second Internal Topic Authority Canary

Classification: `SECOND_CANARY_OPERATIONALLY_CLEAN`

Cases completed/provider calls/valid responses: 5 / 3 / 3

Authority applied/consumed: 2 / 2

Global mode before/after: `SHADOW` / `SHADOW`

Next step: `PAUSE_FOR_HUMAN_AUDIT`

Only sanitized provenance is persisted. No source body, raw prompt, raw response,
provider exception payload, secret, or reasoning text is stored.
